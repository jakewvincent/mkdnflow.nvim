# Mkdnflow Documentation Sections
# This file defines the complete documentation structure with all prose content

sections:
  # =====================================================================
  # INTRODUCTION
  # =====================================================================
  - title: Introduction
    tag: intro
    emoji: "🚀"
    content:
      - type: prose
        text: |
          Mkdnflow is designed for the *fluent* navigation and management of
          [markdown](https://markdownguide.org) documents and document collections
          (notebooks, wikis, etc). It features numerous convenience functions that
          make it easier to work within raw markdown documents or document collections:
          link and reference handling (see `link-handling`), navigation
          (see `navigation`), table support (see `table-support`), list
          (see `list-support`) and to-do list (see `todo-support`) support, file
          management (see `file-mgmt`), section folding (see `folding`), and more.
          Use it for notetaking, personal knowledge management, static website
          building, and more. Most features are highly tweakable (see `configuration`).

  # =====================================================================
  # FEATURES
  # =====================================================================
  - title: Features
    tag: features
    emoji: "✨"
    children:
      - title: Navigation
        tag: navigation
        emoji: "🧭"
        children:
          - title: Within-buffer navigation
            tag: nav-buffer
            content:
              - type: list
                items:
                  - text: Jump to links
                    done: true
                  - text: Jump to section headings
                    done: true
              - type: responsive_image
                light: https://raw.githubusercontent.com/jakewvincent/mkdnflow-media/main/demos/navigation_light.gif
                dark: https://raw.githubusercontent.com/jakewvincent/mkdnflow-media/main/demos/navigation_dark.gif
                alt: In-buffer navigation demo

          - title: Within-notebook navigation
            tag: nav-notebook
            content:
              - type: list
                items:
                  - text: Link following
                    done: true
                    children:
                      - text: Open markdown and other text filetypes in the current window
                        done: true
                      - text: Open other filetypes and URLs with your system's default application
                        done: true
                  - text: "Browser-like 'Back' and 'Forward' functionality"
                    done: true
                  - text: Table of contents window
                    done: false

      - title: Link and reference handling
        tag: link-handling
        emoji: "🔗"
        content:
          - type: list
            items:
              - text: Link creation from a visual selection or the text under the cursor
                done: true
              - text: Link destruction
                done: true
              - text: Follow links to local paths and other Markdown files
                done: true
              - text: Follow external links (open using default application)
                done: true
              - text: Follow `.bib`-based references
                done: true
                children:
                  - text: Open `url` or `doi` field in the default browser
                    done: true
                  - text: Open documents specified in `file` field
                    done: true
              - text: Implicit filetype extensions
                done: true
              - text: Support for various link types
                done: true
                children:
                  - text: "Standard Markdown links (`[my page](my_page.md)`)"
                    done: true
                  - text: "Wiki links (direct `[[my page]]` or piped `[[my_page.md|my page]]`)"
                    done: true
                  - text: "Automatic links (`<https://my.page>`)"
                    done: true
                  - text: "Reference-style links (`[my page][1]` with `[1]: my_page.md`)"
                    done: true
                  - text: "Image links (`![alt text](image.png)`) — opened in system viewer"
                    done: true
                  - text: "Citations (`@citekey` or Pandoc-style `[@citekey]`)"
                    done: true
                    children:
                      - text: "Visual selection of a citation + `<CR>` creates a link from the citekey"
                        done: true
                  - text: "Footnotes (`[^1]` with `[^1]: Footnote text`)"
                    done: true
          - type: responsive_image
            light: https://raw.githubusercontent.com/jakewvincent/mkdnflow-media/main/demos/links_light.gif
            dark: https://raw.githubusercontent.com/jakewvincent/mkdnflow-media/main/demos/links_dark.gif
            alt: Link lifecycle demo

      - title: Table support
        tag: table-support
        emoji: "📊"
        content:
          - type: list
            items:
              - text: Table creation
                done: true
              - text: Table extension (add rows and columns)
                done: true
              - text: Table formatting
                done: true
              - text: Pandoc grid table support
                done: true
              - text: Column alignment (left, right, center)
                done: true
              - text: Paste delimited data as a table
                done: true
              - text: Import delimited file into a new table
                done: true
          - type: responsive_image
            light: https://raw.githubusercontent.com/jakewvincent/mkdnflow-media/main/demos/tables_light.gif
            dark: https://raw.githubusercontent.com/jakewvincent/mkdnflow-media/main/demos/tables_dark.gif
            alt: Table workflow demo

      - title: List support
        tag: list-support
        emoji: "📝"
        content:
          - type: list
            items:
              - text: Automatic list extension
                done: true
              - text: Sensible auto-indentation and auto-dedentation
                done: true
              - text: Ordered list number updating
                done: true
          - type: responsive_image
            light: https://raw.githubusercontent.com/jakewvincent/mkdnflow-media/main/demos/lists_light.gif
            dark: https://raw.githubusercontent.com/jakewvincent/mkdnflow-media/main/demos/lists_dark.gif
            alt: List management demo

      - title: To-do list support
        tag: todo-support
        emoji: "✅"
        content:
          - type: list
            items:
              - text: Toggle to-do item status
                done: true
              - text: Status propagation
                done: true
              - text: To-do list sorting
                done: true
              - text: Create to-do items from plain ordered or unordered list items
                done: true
              - text: Customizable highlighting for to-do status markers and content
                done: true
          - type: responsive_image
            light: https://raw.githubusercontent.com/jakewvincent/mkdnflow-media/main/demos/todo_light.gif
            dark: https://raw.githubusercontent.com/jakewvincent/mkdnflow-media/main/demos/todo_dark.gif
            alt: To-do list demo

      - title: File management
        tag: file-mgmt
        emoji: "📁"
        content:
          - type: list
            items:
              - text: Simultaneous link and file renaming
                done: true
              - text: As-needed directory creation
                done: true

      - title: Folding
        tag: folding
        emoji: "🪗"
        content:
          - type: list
            items:
              - text: Section folding and fold toggling
                done: true
              - text: Helpful indicators for folded section contents
                done: true
                children:
                  - text: Section heading level
                    done: true
                  - text: Counts of Markdown objects (tables, lists, code blocks, etc.)
                    done: true
                  - text: Line and word counts
                    done: true
              - text: YAML block folding
                done: false
          - type: responsive_image
            light: https://raw.githubusercontent.com/jakewvincent/mkdnflow-media/main/demos/folding_light.gif
            dark: https://raw.githubusercontent.com/jakewvincent/mkdnflow-media/main/demos/folding_dark.gif
            alt: Section folding demo

      - title: Completion
        tag: completion
        emoji: "🔮"
        content:
          - type: list
            items:
              - text: Path completion
                done: true
              - text: Completion of bibliography items
                done: true

      - title: YAML block parsing
        tag: yaml-parsing
        emoji: "🧩"
        content:
          - type: list
            items:
              - text: Specify a bibliography file in YAML front matter
                done: true

      - title: Visual enhancements
        tag: visual
        emoji: "🖌️"
        content:
          - type: list
            items:
              - text: Conceal markdown and wiki link syntax
                done: true
              - text: Extended link highlighting
                done: false
                children:
                  - text: Automatic links
                    done: false
                  - text: Wiki links
                    done: false

  # =====================================================================
  # INSTALLATION
  # =====================================================================
  - title: Installation
    tag: installation
    emoji: "💾"
    content:
      - type: prose
        text: |
          **Requirements**:

          * Linux, macOS, or Windows
          * Neovim >= 0.9.5 (tested on 0.9.5, 0.10.x, and stable)

          Install Mkdnflow using your preferred package manager for Neovim. Once installed,
          Mkdnflow is configured and initialized using a setup function.
      - type: collapsible
        summary: Install with Lazy
        content:
          - type: code
            language: lua
            code: |
              require('lazy').setup({
                  -- Your other plugins
                  {
                      'jakewvincent/mkdnflow.nvim',
                      ft = { 'markdown', 'rmd' },  -- Add custom filetypes here if configured
                      config = function()
                          require('mkdnflow').setup({
                              -- Your config
                          })
                      end
                  }
                  -- Your other plugins
              })
      - type: collapsible
        summary: Install with Vim-Plug
        content:
          - type: code
            language: vim
            code: |
              " Vim-Plug
              Plug 'jakewvincent/mkdnflow.nvim'

              " Include the setup function somewhere else in your init.vim file, or the
              " plugin won't activate itself:
              lua << EOF
              require('mkdnflow').setup({
                  -- Config goes here; leave blank for defaults
              })
              EOF

  # =====================================================================
  # CONFIGURATION (includes all config subsections)
  # =====================================================================
  - title: Configuration
    tag: configuration
    emoji: "⚙️"
    children:
      - title: Quick start
        tag: quickstart
        emoji: "⚡"
        content:
          - type: prose
            text: |
              Mkdnflow is configured and initialized using a setup function. To use
              the default settings, pass no arguments or an empty table to the setup function:
          - type: code
            language: lua
            code: |
              {
                  'jakewvincent/mkdnflow.nvim',
                  config = function()
                      require('mkdnflow').setup({})
                  end
              }

      - title: Advanced configuration and sample recipes
        tag: config-advanced
        emoji: "🔧"
        content:
          - type: prose
            text: |
              Most features are highly configurable. Study the default config first
              and read the documentation for the configuration options below or in
              the help files.
          - type: collapsible
            summary: "🔧 Complete default config"
            content:
              - type: default_config
        children:
          - title: Configuration options
            tag: config-options
            emoji: "🎨"
            children:
              - title: modules
                tag: config-modules
                content:
                  - type: code
                    language: lua
                    code: |
                      require('mkdnflow').setup({
                          modules = {
                              bib = true,
                              buffers = true,
                              conceal = true,
                              cursor = true,
                              folds = true,
                              foldtext = true,
                              links = true,
                              lists = true,
                              maps = true,
                              paths = true,
                              tables = true,
                              to_do = true,
                              yaml = false,
                              cmp = false,
                          notebook = true,
                          }
                      })
                  - type: config_table
                    ref: modules

              - title: create_dirs
                tag: config-create-dirs
                content:
                  - type: code
                    language: lua
                    code: |
                      require('mkdnflow').setup({
                          create_dirs = true,
                      })
                  - type: config_table
                    ref: create_dirs

              - title: path_resolution
                tag: config-path-resolution
                content:
                  - type: code
                    language: lua
                    code: |
                      require('mkdnflow').setup({
                          path_resolution = {
                              primary = 'first',
                              fallback = 'current',
                              root_marker = false,
                              sync_cwd = false,
                              update_on_navigate = false,
                          },
                      })
                  - type: config_table
                    ref: path_resolution

              - title: filetypes
                tag: config-filetypes
                content:
                  - type: code
                    language: lua
                    code: |
                      require('mkdnflow').setup({
                          filetypes = {
                              markdown = true,
                              rmd = true,
                          },
                      })
                  - type: config_table
                    ref: filetypes
                  - type: admonition
                    kind: note
                    content: |
                      Mkdnflow activates based on Neovim's filetype detection, not file extensions.
                      This means files with modelines like `vim: ft=markdown` will activate the plugin
                      regardless of their extension.

                      **Lazy loading note**: If you use a plugin manager with lazy loading (e.g., `ft = { 'markdown' }`)
                      and configure custom extensions like `wiki = true`, you must add those filetypes
                      to your lazy loading configuration (e.g., `ft = { 'markdown', 'wiki' }`), since the
                      plugin won't load until it sees a matching filetype.

              - title: wrap
                tag: config-wrap
                content:
                  - type: code
                    language: lua
                    code: |
                      require('mkdnflow').setup({
                          wrap = false,
                      })
                  - type: config_table
                    ref: wrap

              - title: bib
                tag: config-bib
                content:
                  - type: code
                    language: lua
                    code: |
                      require('mkdnflow').setup({
                          bib = {
                              default_path = nil,
                              find_in_root = true,
                          },
                      })
                  - type: config_table
                    ref: bib

              - title: silent
                tag: config-silent
                content:
                  - type: code
                    language: lua
                    code: |
                      require('mkdnflow').setup({
                          silent = false,
                      })
                  - type: config_table
                    ref: silent

              - title: cursor
                tag: config-cursor
                content:
                  - type: code
                    language: lua
                    code: |
                      require('mkdnflow').setup({
                          cursor = {
                              jump_patterns = nil,
                          },
                      })
                  - type: config_table
                    ref: cursor

              - title: links
                tag: config-links
                content:
                  - type: code
                    language: lua
                    code: |
                      require('mkdnflow').setup({
                          links = {
                              style = 'markdown',
                              compact = false,
                              conceal = false,
                              search_range = 0,
                              implicit_extension = nil,
                              transform_on_follow = false,
                              transform_on_create = function(text)
                                  text = text:gsub(" ", "-")
                                  text = text:lower()
                                  text = os.date('%Y-%m-%d_') .. text
                                  return(text)
                              end,
                              auto_create = true,
                              on_create_new = false,
                          },
                      })
                  - type: config_table
                    ref: links
                  - type: collapsible
                    summary: Sample links recipes
                    content:
                      - type: code
                        language: lua
                        code: |
                          require('mkdnflow').setup({
                              links = {
                                  -- If you want all link paths to be explicitly prefixed with the year
                                  -- and for the path to be converted to uppercase:
                                  transform_on_create = function(input)
                                      return(string.upper(os.date('%Y-')..input))
                                  end,
                                  -- Link paths that match a date pattern can be opened in a `journals`
                                  -- subdirectory of your notebook, and all others can be opened in a
                                  -- `pages` subdirectory:
                                  transform_on_follow = function(input)
                                      if input:match('%d%d%d%d%-%d%d%-%d%d') then
                                          return('journals/'..input)
                                      else
                                          return('pages/'..input)
                                      end
                                  end
                              }
                          })
                      - type: prose
                        text: |
                          **Delegate new-file creation to [zk](https://github.com/zk-org/zk):**
                      - type: code
                        language: lua
                        code: |
                          require('mkdnflow').setup({
                              links = {
                                  on_create_new = function(path, title)
                                      -- Let zk create the note with its own templates and ID scheme.
                                      -- `zk new` prints the created path to stdout.
                                      local dir = vim.fn.fnamemodify(path, ':h')
                                      local cmd = { 'zk', 'new', '--no-input', '--print-path', dir }
                                      if title then
                                          table.insert(cmd, '--title')
                                          table.insert(cmd, title)
                                      end
                                      local result = vim.fn.system(cmd)
                                      local new_path = vim.trim(result)
                                      if vim.v.shell_error ~= 0 then
                                          vim.notify('zk new failed: ' .. result, vim.log.levels.ERROR)
                                          return nil
                                      end
                                      return new_path  -- mkdnflow opens the zk-created file
                                  end,
                              },
                          })

              - title: new_file_template
                tag: config-new-file-template
                content:
                  - type: code
                    language: lua
                    code: |
                      require('mkdnflow').setup({
                          new_file_template = {
                              enabled = false,
                              placeholders = {
                                  before = { title = 'link_title', date = 'os_date' },
                                  after = {},
                              },
                              template = '# {{ title }}',
                          },
                      })
                  - type: config_table
                    ref: new_file_template

              - title: to_do
                tag: config-to-do
                content:
                  - type: code
                    language: lua
                    code: |
                      require('mkdnflow').setup({
                          to_do = {
                              highlight = false,
                              statuses = {
                                  not_started = { marker = ' ', ... },
                                  in_progress = { marker = '-', ... },
                                  complete = { marker = { 'X', 'x' }, ... },
                              },
                              status_order = { 'not_started', 'in_progress', 'complete' },
                              status_propagation = { up = true, down = true },
                              sort = {
                                  on_status_change = false,
                                  recursive = false,
                                  cursor_behavior = { track = true },
                              },
                          },
                      })
                  - type: config_table
                    ref: to_do
                  - type: admonition
                    kind: warning
                    content: |
                      The following to-do configuration options are deprecated. Please use the
                      `to_do.statuses` dict and `to_do.status_order` instead. Continued support
                      for these options is temporarily provided by a compatibility layer that will
                      be removed in the near future.

                      * `to_do.symbols` - A list of markers representing to-do completion statuses
                      * `to_do.not_started` - Which marker represents a not-yet-started to-do
                      * `to_do.in_progress` - Which marker represents an in-progress to-do
                      * `to_do.complete` - Which marker represents a complete to-do
                      * `to_do.update_parents` - Whether parent to-dos' statuses should be updated
                      * `to_do.statuses` (array form) - The old array-of-tables format for statuses
                      * `to_do.statuses[*].skip_on_toggle` - Use `status_order` membership instead
                      * `to_do.statuses[*].exclude_from_rotation` - Use `status_order` membership instead

              - title: foldtext
                tag: config-foldtext
                content:
                  - type: code
                    language: lua
                    code: |
                      require('mkdnflow').setup({
                          foldtext = {
                              object_count = true,
                              object_count_icon_set = 'emoji',
                              object_count_opts = function()
                                  return require('mkdnflow').foldtext.default_count_opts()
                              end,
                              line_count = true,
                              line_percentage = true,
                              word_count = false,
                              title_transformer = function()
                                  return require('mkdnflow').foldtext.default_title_transformer
                              end,
                              fill_chars = {
                                  left_edge = '⢾⣿⣿',
                                  right_edge = '⣿⣿⡷',
                                  item_separator = ' · ',
                                  section_separator = ' ⣹⣿⣏ ',
                                  left_inside = ' ⣹',
                                  right_inside = '⣏ ',
                                  middle = '⣿',
                              },
                          },
                      })
                  - type: config_table
                    ref: foldtext
                  - type: collapsible
                    summary: Sample foldtext recipes
                    content:
                      - type: code
                        language: lua
                        code: |
                          -- SAMPLE FOLDTEXT CONFIGURATION RECIPE WITH COMMENTS
                          require('mkdnflow').setup({
                              foldtext = {
                                  title_transformer = function()
                                      local function my_title_transformer(text)
                                          local updated_title = text:gsub('%b{}', '')
                                          updated_title = updated_title:gsub('^%s*', '')
                                          updated_title = updated_title:gsub('%s*$', '')
                                          updated_title = updated_title:gsub('^######', '░░░░░▓')
                                          updated_title = updated_title:gsub('^#####', '░░░░▓▓')
                                          updated_title = updated_title:gsub('^####', '░░░▓▓▓')
                                          updated_title = updated_title:gsub('^###', '░░▓▓▓▓')
                                          updated_title = updated_title:gsub('^##', '░▓▓▓▓▓')
                                          updated_title = updated_title:gsub('^#', '▓▓▓▓▓▓')
                                          return updated_title
                                      end
                                      return my_title_transformer
                                  end,
                                  object_count_icon_set = 'nerdfont',
                                  object_count_opts = function()
                                      local opts = {
                                          link = false,
                                          blockquote = {
                                              icon = ' ',
                                              count_method = {
                                                  pattern = { '^>.+$' },
                                                  tally = 'blocks',
                                              }
                                          },
                                          fncblk = { icon = ' ' }
                                      }
                                      return opts
                                  end,
                                  line_count = false,
                                  word_count = true,
                                  fill_chars = {
                                      left_edge = '╾─🖿 ─',
                                      right_edge = '──╼',
                                      item_separator = ' · ',
                                      section_separator = ' // ',
                                      left_inside = ' ┝',
                                      right_inside = '┥ ',
                                      middle = '─',
                                  },
                              },
                          })
                      - type: prose
                        text: |
                          The above recipe will produce foldtext like the following
                          (for an h3-level section heading called `My section`):
                      - type: responsive_image
                        light: https://raw.githubusercontent.com/jakewvincent/mkdnflow-media/main/assets/foldtext/foldtext_ex.png
                        dark: https://raw.githubusercontent.com/jakewvincent/mkdnflow-media/main/assets/foldtext/foldtext_ex_dark.png
                        alt: Enhanced foldtext example
                        centered: true

              - title: tables
                tag: config-tables
                content:
                  - type: code
                    language: lua
                    code: |
                      require('mkdnflow').setup({
                          tables = {
                              type = 'pipe',
                              trim_whitespace = true,
                              format_on_move = true,
                              auto_extend_rows = false,
                              auto_extend_cols = false,
                              style = {
                                  cell_padding = 1,
                                  separator_padding = 1,
                                  outer_pipes = true,
                                  apply_alignment = true,
                              },
                          },
                      })
                  - type: config_table
                    ref: tables

              - title: yaml
                tag: config-yaml
                content:
                  - type: code
                    language: lua
                    code: |
                      require('mkdnflow').setup({
                          yaml = {
                              bib = { override = false },
                          },
                      })
                  - type: config_table
                    ref: yaml

              - title: mappings
                tag: config-mappings
                content:
                  - type: code
                    language: lua
                    code: |
                      require('mkdnflow').setup({
                          mappings = {
                              MkdnEnter = { { 'n', 'v' }, '<CR>' },
                              MkdnGoBack = { 'n', '<BS>' },
                              MkdnGoForward = { 'n', '<Del>' },
                              MkdnMoveSource = { 'n', '<F2>' },
                              MkdnNextLink = { 'n', '<Tab>' },
                              MkdnPrevLink = { 'n', '<S-Tab>' },
                              MkdnFollowLink = false,
                              MkdnDestroyLink = { 'n', '<M-CR>' },
                              MkdnTagSpan = { 'v', '<M-CR>' },
                              MkdnYankAnchorLink = { 'n', 'yaa' },
                              MkdnYankFileAnchorLink = { 'n', 'yfa' },
                              MkdnNextHeading = { 'n', ']]' },
                              MkdnPrevHeading = { 'n', '[[' },
                              MkdnIncreaseHeading = { { 'n', 'v' }, '+' },
                              MkdnDecreaseHeading = { { 'n', 'v' }, '-' },
                              MkdnIncreaseHeadingOp = { { 'n', 'v' }, 'g+' },
                              MkdnDecreaseHeadingOp = { { 'n', 'v' }, 'g-' },
                              MkdnToggleToDo = { { 'n', 'v' }, '<C-Space>' },
                              MkdnNewListItem = false,
                              MkdnNewListItemBelowInsert = { 'n', 'o' },
                              MkdnNewListItemAboveInsert = { 'n', 'O' },
                              MkdnExtendList = false,
                              MkdnUpdateNumbering = { 'n', '<leader>nn' },
                              MkdnTableNextCell = { 'i', '<Tab>' },
                              MkdnTablePrevCell = { 'i', '<S-Tab>' },
                              MkdnTableNextRow = false,
                              MkdnTablePrevRow = { 'i', '<M-CR>' },
                              MkdnTableNewRowBelow = { 'n', '<leader>ir' },
                              MkdnTableNewRowAbove = { 'n', '<leader>iR' },
                              MkdnTableNewColAfter = { 'n', '<leader>ic' },
                              MkdnTableNewColBefore = { 'n', '<leader>iC' },
                              MkdnTableDeleteRow = { 'n', '<leader>dr' },
                              MkdnTableDeleteCol = { 'n', '<leader>dc' },
                              MkdnTableAlignLeft = { 'n', '<leader>al' },
                              MkdnTableAlignRight = { 'n', '<leader>ar' },
                              MkdnTableAlignCenter = { 'n', '<leader>ac' },
                              MkdnTableAlignDefault = { 'n', '<leader>ax' },
                              MkdnFoldSection = { 'n', '<leader>f' },
                              MkdnUnfoldSection = { 'n', '<leader>F' },
                              MkdnTab = false,
                              MkdnSTab = false,
                              MkdnIndentListItem = { 'i', '<C-t>' },
                              MkdnDedentListItem = { 'i', '<C-d>' },
                              MkdnCreateLink = false,
                              MkdnCreateLinkFromClipboard = { { 'n', 'v' }, '<leader>p' },
                          },
                      })
                  - type: prose
                    text: |
                      See descriptions of commands and mappings below.

                      **Note**: `<command>` should be the name of a command defined in
                      `mkdnflow.nvim/plugin/mkdnflow.lua` (see `:h Mkdnflow-commands` for a list).
                  - type: config_table
                    ref: mappings

          - title: Completion setup
            tag: completion-setup
            emoji: "🔮"
            content:
              - type: prose
                text: |
                  Mkdnflow provides completion for file links, bib citations (`@`), footnotes
                  (`[^`), and heading anchors (`](#` / `[[#`). Both **nvim-cmp** and **blink.cmp**
                  are supported.
              - type: prose
                text: |
                  #### nvim-cmp

                  Set `modules.completion = true` in your mkdnflow config, then add `mkdnflow`
                  as a source in your `cmp` setup:
              - type: code
                language: lua
                code: |
                  -- mkdnflow setup
                  require('mkdnflow').setup({
                      modules = { completion = true },
                  })

                  -- nvim-cmp setup
                  cmp.setup({
                      sources = cmp.config.sources({
                          { name = 'mkdnflow' },
                      }),
                      formatting = {
                          format = function(entry, vim_item)
                              vim_item.menu = ({
                                  mkdnflow = '[Mkdnflow]',
                              })[entry.source_name]
                              return vim_item
                          end
                      }
                  })
              - type: prose
                text: |
                  #### blink.cmp

                  Set `modules.completion = true` in your mkdnflow config, then register the
                  source in your blink.cmp setup:
              - type: code
                language: lua
                code: |
                  -- mkdnflow setup
                  require('mkdnflow').setup({
                      modules = { completion = true },
                  })

                  -- blink.cmp setup
                  require('blink.cmp').setup({
                      sources = {
                          default = { 'lsp', 'mkdnflow' },
                          providers = {
                              mkdnflow = {
                                  name = 'Mkdnflow',
                                  module = 'mkdnflow.completion.blink',
                              },
                          },
                      },
                  })
              - type: admonition
                kind: warning
                content: |
                  There may be some compatibility issues with the completion module and
                  `links.transform_on_create`/`links.transform_on_follow` functions.

                  If you have some `transform_on_create` option for links to organizing in folders
                  then the folder name will be inserted accordingly. Some transformations may not
                  work as expected in completions.

                  To prevent this, make sure you write sensible transformation functions,
                  preferably using it for folder organization.

  # =====================================================================
  # COMMANDS & MAPPINGS
  # =====================================================================
  - title: Commands & mappings
    tag: commands
    emoji: "🛠️"
    content:
      - type: prose
        text: |
          Below are descriptions of the user commands defined by Mkdnflow. For the
          default mappings to these commands, see the `mappings = ...` section of
          Configuration options.
      - type: command_table
      - type: admonition
        kind: tip
        content: |
          If you are attempting to (re)map `<CR>` in insert mode but can't get it to
          work, try inspecting your current insert mode mappings and seeing if anything
          is overriding your mapping. Possible candidates are completion plugins and
          auto-pair plugins.

          If using nvim-cmp, consider using the mapping with a fallback.
          If using an autopair plugin that automatically maps `<CR>` (e.g. nvim-autopairs),
          see if it provides a way to disable its `<CR>` mapping.
    children:
      - title: Custom mappings
        tag: custom-mappings
        content:
          - type: prose
            text: |
              There are several ways to customize mkdnflow's keybindings. All
              mkdnflow commands (e.g., `MkdnFollowLink`, `MkdnToggleToDo`) are
              standard Neovim user commands and can be mapped using any method
              you'd normally use to map a Neovim command.
        children:
          - title: Disabling default mappings
            tag: custom-mappings-disable
            content:
              - type: prose
                text: |
                  To disable a single default mapping, set it to `false` in the
                  `mappings` table:
              - type: code
                language: lua
                code: |
                  require('mkdnflow').setup({
                      mappings = {
                          MkdnNextLink = false,              -- Frees <Tab>
                          MkdnPrevLink = false,              -- Frees <S-Tab>
                          MkdnNewListItemBelowInsert = false, -- Frees 'o'
                          MkdnNewListItemAboveInsert = false, -- Frees 'O'
                      },
                  })
              - type: prose
                text: |
                  To disable *all* default mappings at once, disable the `maps`
                  module:
              - type: code
                language: lua
                code: |
                  require('mkdnflow').setup({
                      modules = { maps = false },
                  })
          - title: Remapping via the mappings table
            tag: custom-mappings-remap
            content:
              - type: prose
                text: |
                  The simplest way to change a binding is to set a different key
                  in the `mappings` table. Each entry takes a mode (or array of
                  modes) and a key string:
              - type: code
                language: lua
                code: |
                  require('mkdnflow').setup({
                      mappings = {
                          MkdnFollowLink = { 'n', 'gf' },         -- Single mode
                          MkdnToggleToDo = { { 'n', 'v' }, 'tt' }, -- Multiple modes
                          MkdnEnter = false,                       -- Disable default
                      },
                  })
              - type: admonition
                kind: note
                content: |
                  The `mappings` table maps each command to a single key. If you
                  need the same command on multiple keys (e.g., in different
                  modes), use `on_attach`, lazy.nvim `keys`, or a manual autocmd
                  instead.
          - title: Using on_attach
            tag: custom-mappings-on-attach
            content:
              - type: prose
                text: |
                  The `on_attach` callback fires each time mkdnflow activates on
                  a matching buffer. It receives the buffer number as its
                  argument, making it ideal for buffer-local mappings, options,
                  or autocommands.

                  When `modules.maps` is enabled (the default), default mappings
                  are set up *before* `on_attach` runs, so you can selectively
                  override or supplement them:
              - type: code
                language: lua
                code: |
                  require('mkdnflow').setup({
                      on_attach = function(bufnr)
                          local opts = { buffer = bufnr }

                          -- Bind a command to an additional key
                          vim.keymap.set('n', 'gf', '<Cmd>MkdnFollowLink<CR>', opts)

                          -- Bind the same command in multiple modes
                          vim.keymap.set({ 'n', 'v' }, 'tt', '<Cmd>MkdnToggleToDo<CR>', opts)

                          -- Add a mapping not in the defaults
                          vim.keymap.set('n', '<leader>ml', '<Cmd>MkdnCreateLink<CR>', opts)

                          -- Delete a default mapping you don't want
                          pcall(vim.keymap.del, 'n', '+', { buffer = bufnr })

                          -- Set buffer-local options
                          vim.bo[bufnr].textwidth = 80
                      end,
                  })
              - type: prose
                text: |
                  To use `on_attach` as the *sole* source of mappings (fully
                  manual control), disable the `maps` module:
              - type: code
                language: lua
                code: |
                  require('mkdnflow').setup({
                      modules = { maps = false },
                      on_attach = function(bufnr)
                          local opts = { buffer = bufnr }
                          vim.keymap.set('n', '<CR>', '<Cmd>MkdnEnter<CR>', opts)
                          vim.keymap.set('n', '<BS>', '<Cmd>MkdnGoBack<CR>', opts)
                          vim.keymap.set('n', '<Tab>', '<Cmd>MkdnNextLink<CR>', opts)
                          vim.keymap.set({ 'n', 'v' }, '<C-Space>', '<Cmd>MkdnToggleToDo<CR>', opts)
                      end,
                  })
              - type: prose
                text: |
                  You can also use `on_attach` to create links in both markdown
                  and wiki styles from different keys. The `MkdnCreateLink` and
                  `MkdnCreateLinkFromClipboard` commands accept an optional
                  style argument (abbreviations like `w` and `m` are accepted):
              - type: code
                language: lua
                code: |
                  require('mkdnflow').setup({
                      on_attach = function(bufnr)
                          local opts = { buffer = bufnr }
                          -- Markdown link from clipboard (uses configured default)
                          vim.keymap.set({ 'n', 'v' }, '<leader>p',
                              '<Cmd>MkdnCreateLinkFromClipboard<CR>', opts)
                          -- Wiki link from clipboard (per-call style override)
                          vim.keymap.set({ 'n', 'v' }, '<leader>wp',
                              '<Cmd>MkdnCreateLinkFromClipboard wiki<CR>', opts)
                      end,
                  })
          - title: Using lazy.nvim keys
            tag: custom-mappings-lazy
            content:
              - type: prose
                text: |
                  If you use lazy.nvim, you can define mappings in the plugin
                  spec's `keys` table. Use the `ft` field to scope them to
                  markdown buffers:
              - type: code
                language: lua
                code: |
                  {
                      'jakewvincent/mkdnflow.nvim',
                      ft = { 'markdown', 'rmd' },
                      opts = {
                          mappings = {
                              MkdnFollowLink = false, -- Disable default
                              MkdnEnter = false,
                          },
                      },
                      keys = {
                          { 'gf', '<Cmd>MkdnFollowLink<CR>', ft = 'markdown', desc = 'Follow link' },
                          { '<CR>', '<Cmd>MkdnEnter<CR>', ft = 'markdown', desc = 'Mkdn enter' },
                      },
                  }
              - type: admonition
                kind: note
                content: |
                  Lazy.nvim's `ft`-scoped `keys` use a filetype autocmd
                  internally, so they are effectively buffer-local. Plain `keys`
                  entries without `ft` are global and will apply in non-markdown
                  buffers too.
          - title: Manual autocmd
            tag: custom-mappings-autocmd
            content:
              - type: prose
                text: |
                  You can also set mappings via your own `FileType` autocmd.
                  This is equivalent to `on_attach` but managed outside of
                  mkdnflow's config:
              - type: code
                language: lua
                code: |
                  vim.api.nvim_create_autocmd('FileType', {
                      pattern = { 'markdown', 'rmd' },
                      callback = function(args)
                          vim.keymap.set('n', 'gf', '<Cmd>MkdnFollowLink<CR>', {
                              buffer = args.buf,
                              desc = 'Follow link',
                          })
                      end,
                  })
              - type: admonition
                kind: warning
                content: |
                  With a manual autocmd, firing order depends on registration
                  order. Your autocmd may fire before or after mkdnflow's,
                  depending on when each is registered. Use `on_attach` for
                  guaranteed ordering (it always runs after default mappings).

  # =====================================================================
  # API
  # =====================================================================
  - title: API
    tag: api
    emoji: "📚"
    content:
      - type: prose
        text: |
          Mkdnflow provides a range of Lua functions that can be called directly to
          manipulate markdown files, navigate through buffers, manage links, and more.
          Below are the primary functions available:
      - type: list
        ordered: true
        items:
          - text: Initialization (see `initialization`)
          - text: Link management (see `linkmanagement`)
          - text: Link & path handling (see `linkpathhandling`)
          - text: Templates (see `templates`)
          - text: Buffer navigation (see `buffernavigation`)
          - text: Cursor movement (see `cursormovement`)
          - text: Cursor-aware manipulations (see `cursorawaremanipulations`)
          - text: List management (see `listmanagement`)
          - text: To-do list management (see `todolistmanagement`)
          - text: Table management (see `tablemanagement`)
          - text: Folds (see `folds`)
          - text: Yaml blocks (see `yamlblocks`)
          - text: Notebook primitives (see `notebook`)
          - text: Bibliography (see `bibliography`)
          - text: Statusline components (see `statusline`)
    children:
      - title: Initialization
        tag: initialization
        content:
          - type: api_section
            ref: initialization

      - title: Link management
        tag: linkmanagement
        content:
          - type: api_section
            ref: linkmanagement

      - title: Link and path handling
        tag: linkpathhandling
        content:
          - type: api_section
            ref: linkpathhandling

      - title: Templates
        tag: templates
        content:
          - type: api_section
            ref: templates

      - title: Buffer navigation
        tag: buffernavigation
        content:
          - type: api_section
            ref: buffernavigation

      - title: Cursor movement
        tag: cursormovement
        content:
          - type: api_section
            ref: cursormovement

      - title: Cursor-aware manipulations
        tag: cursorawaremanipulations
        content:
          - type: api_section
            ref: cursorawaremanipulations

      - title: List management
        tag: listmanagement
        content:
          - type: api_section
            ref: listmanagement
          - type: admonition
            kind: warning
            content: |
              `require('mkdnflow').lists.toggleToDo(opts)` is deprecated. For convenience, it is
              now a wrapper function that calls its replacement, `require('mkdnflow').to_do.toggle_to_do(opts)`.
              See `require('mkdnflow').to_do.toggle_to_do()` for details.

      - title: To-do list management
        tag: todolistmanagement
        content:
          - type: api_section
            ref: todolistmanagement

      - title: Table management
        tag: tablemanagement
        content:
          - type: api_section
            ref: tablemanagement

      - title: Folds
        tag: folds
        content:
          - type: api_section
            ref: folds

      - title: Yaml blocks
        tag: yamlblocks
        content:
          - type: api_section
            ref: yamlblocks

      - title: Notebook primitives
        tag: notebook
        content:
          - type: prose
            text: |
              The following functions provide shared cross-file primitives for scanning
              notebook files, headings, and links. Both synchronous and asynchronous
              variants are available. These serve as building blocks for features like
              backlinks, notebook indexing, and table-of-contents generation.
          - type: api_section
            ref: notebook

      - title: Bibliography
        tag: bibliography
        content:
          - type: api_section
            ref: bibliography

      - title: Statusline components
        tag: statusline
        content:
          - type: prose
            text: |
              The following functions are designed for use in statusline, winbar, or
              tabline components. They return information about the plugin's current
              state and are safe to call frequently (e.g. on every statusline redraw).
          - type: api_section
            ref: statusline
          - type: prose
            text: |
              **Example: lualine component**
          - type: code
            language: lua
            code: |
              require('lualine').setup({
                  sections = {
                      lualine_x = {
                          {
                              function()
                                  local nb = require('mkdnflow').getNotebook()
                                  return nb and nb.name or ''
                              end,
                              cond = function()
                                  return require('mkdnflow').getNotebook() ~= nil
                              end,
                          },
                      },
                  },
              })
          - type: prose
            text: |
              **Example: custom statusline**
          - type: code
            language: lua
            code: |
              vim.o.statusline = '%f %m%=%{%v:lua.require("mkdnflow").getNotebook() '
                  .. '~= nil and " " .. require("mkdnflow").getNotebook().name or ""%}'

  # =====================================================================
  # CONTRIBUTING
  # =====================================================================
  - title: Contributing
    tag: contributing
    emoji: "🤝"
    content:
      - type: prose
        text: |
          See [CONTRIBUTING.md](https://github.com/jakewvincent/mkdnflow.nvim/blob/main/CONTRIBUTING.md)

  # =====================================================================
  # VERSION INFORMATION
  # =====================================================================
  - title: Version information
    tag: versioninformation
    emoji: "🔢"
    content:
      - type: prose
        text: |
          Mkdnflow uses [Semantic Versioning](https://semver.org/). Version numbers
          follow the format MAJOR.MINOR.PATCH:
      - type: list
        items:
          - text: "**MAJOR**: Incompatible API or configuration changes"
          - text: "**MINOR**: New functionality in a backward-compatible manner"
          - text: "**PATCH**: Backward-compatible bug fixes"
      - type: prose
        text: |
          For a detailed history of changes, see
          [CHANGELOG.md](https://github.com/jakewvincent/mkdnflow.nvim/blob/main/CHANGELOG.md).

  # =====================================================================
  # RELATED PROJECTS
  # =====================================================================
  - title: Related projects
    tag: related
    emoji: "🔗"
    children:
      - title: Competition
        tag: related-competition
        content:
          - type: list
            items:
              - text: "[obsidian.nvim](https://github.com/epwalsh/obsidian.nvim)"
              - text: "[wiki.vim](https://github.com/lervag/wiki.vim/)"
              - text: "[Neorg](https://github.com/nvim-neorg/neorg)"
              - text: "[markdown.nvim](https://github.com/tadmccorkle/markdown.nvim)"
              - text: "[Vimwiki](https://github.com/vimwiki/vimwiki)"
              - text: "[follow-md-links.nvim](https://github.com/jghauser/follow-md-links.nvim)"

      - title: Complementary plugins
        tag: related-complementary
        content:
          - type: list
            items:
              - text: "[Obsidian.md](https://obsidian.md)"
              - text: "[clipboard-image.nvim](https://github.com/ekickx/clipboard-image.nvim)"
              - text: "[mdeval.nvim](https://github.com/jubnzv/mdeval.nvim)"
              - text: In-editor rendering
                children:
                  - text: "[render-markdown.nvim](https://github.com/MeanderingProgrammer/render-markdown.nvim)"
                  - text: "[markview.nvim](https://github.com/OXY2DEV/markview.nvim)"
              - text: Preview plugins
                children:
                  - text: "[Markdown Preview for (Neo)vim](https://github.com/iamcco/markdown-preview.nvim)"
                  - text: "[nvim-markdown-preview](https://github.com/davidgranstrom/nvim-markdown-preview)"
                  - text: "[glow.nvim](https://github.com/npxbr/glow.nvim)"
                  - text: "[auto-pandoc.nvim](https://github.com/jghauser/auto-pandoc.nvim)"
